using UnityEngine;
using System.Collections;

public class animPerga : MonoBehaviour {

	/*
	public Animation Anim;
	public AnimationClip element0;
	public AnimationClip element1;
	public AnimationClip element2;
	public AnimationClip element3;
	public float time = 0;

	void Start() {
		Anim = GetComponent<Animation>();
	}
	
	void Update () {
	
		if(RayCast.perga == 1){
		switch (RayCast.pergaminos){

			case 100:

			Anim.PlayQueued(element2.name,QueueMode.PlayNow);
			break;

			case 101:

			Anim.Play (element3.name);
			time += Time.deltaTime;
			if(time >= 0.1){

				time = 0;
				RayCast.pergaminos = 0;
				RayCast.Destruir = true;
			}
			break;
			case 0:

			Anim.CrossFade(element0.name);
			break;
			
			case 1:
				
			Anim.Play(element1.name);
			time += Time.deltaTime;

			if(time >= 0.25){
				time = 0;
				RayCast.pergaminos = 100;
			}
			break;
		}
	 }
  }
  */
}
